CREATE PROCEDURE DATOSHOSPITAL
(@HOSPITAL_COD VARCHAR(50), @PERSONAS INT OUTPUT, @SUMA INT OUTPUT, @MEDIA INT OUTPUT)
AS
SELECT @PERSONAS = COUNT(SALARIO), @SUMA = SUM(CAST(SALARIO AS INT)), @MEDIA = AVG(CAST(SALARIO AS INT)) FROM PLANTILLA
WHERE HOSPITAL_COD = @HOSPITAL_COD